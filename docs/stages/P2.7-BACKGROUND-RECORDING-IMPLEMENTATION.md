# P2.7B Background Recording Implementation

## 1. BASELINE / BRANCH

- 冻结基线：`05ae40e9a939e59210044f54fe15c653f0e15ab5`，可追溯 tag `p2.6-safe-recording-recovery-pass-2026-08-08`。
- 施工分支：`feature/p2.7-background-recording`。
- P2.7A 审计报告原样保留，未 reset、覆盖或删除。

## 2. ROOT CAUSE

`PreviewEngine::ProcessPendingFrame` 在录屏器 HWND 最小化或隐藏时主动关闭并丢弃 WGC frame，截断了 `OutputCanvas -> RenderFrameTap -> Encoder`。WGC `CreateFreeThreaded`/`FrameArrived` 本身持续工作，不是根因。

## 3. ACTUAL FILES CHANGED

- `XbPreview.Native/PreviewEngine.cpp`
- `XbPreview.Native/PreviewRenderer.h`
- `XbPreview.Native/PreviewRenderer.cpp`
- `XbPreview.Native.Tests/NativeTests.cpp`
- 本报告；P2.7A 审计报告随分支保留。

## 4. IMPLEMENTATION

移除 visibility 导致的整帧提前丢弃。Native 私有 `PreviewRenderer::RenderFrame` 增加 `presentationEnabled` 参数：每帧仍完成既有 OutputCanvas 合成、cursor 和一次 `RenderFrameTap::ObserveAndCopy`；不可见时仅在 Tap 后返回，跳过 Preview back-buffer draw/Present。未新增公开 ABI、capture/render/recording pipeline 或跨语言依赖。

## 5. BUILD / TEST RESULTS

- Release x64 solution Build：PASS；一次工具超时留下零字节生成对象，精确删除该可再生对象后增量 Build PASS。
- 最终 `XbPreview.Native.Tests.vcxproj` Release x64 增量 Build：PASS。
- `--p2.7b-background-recording` 最终 PASS：Capture=170、Present=43、Dropped=1、Encoded=145、Duration100ns=25,331,437、Finalize=1、Residual=0。
- 捆绑默认全量模式不作为本切片门禁：它进入无关 P2.6 祖先测试，并暴露基线 Manual Tap 无 consumer 的夹具矛盾及 `stop-during-starting` 输出目录竞态；未修改冻结产品代码或把它们写成 PASS。

## 6. MINIMIZE FACTS

- Gate 1：125.013 秒；最小化约 65 秒，WGC 约 60 FPS，Present 固定，样本窗 PTS 增长 54.513 秒；恢复后继续 Present。
- Gate 3：最小化 280.2 秒，Capture +11,794，Present +0；录制未重启。

## 7. HIDE FACTS

- Gate 2：两次隐藏各约 70 秒；隐藏样本窗 PTS 分别增长 59.996/57.996 秒，Present 停止，Capture/Tap/Encoder 继续。
- Gate 3：隐藏 450.2 秒，Capture +26,725，Present +0，dropped 未继续增长。

## 8. RESTORE FACTS

Gate 1 Restore、Gate 2 两次 Show、Gate 3 中途 Restore 后再次 Hide 及最终 Restore 均由下一批有效帧恢复 Present；无 Stop/Restart Recording、Encoder 或 WGC。

## 9. CAPTURE / OUTPUTCANVAS / TAP / ENCODER FACTS

Gate 3 共 Capture 49,165；Tap observed/copied/enqueued/returned 为 49,062/49,040/49,040/49,040；Encoder input/submitted 为 49,040/48,984。单一 OutputCanvas、单次 Tap、单一 Encoder consumer 合同不变。

## 10. PTS / DROPPED / BACKPRESSURE

Gate 1/2/3 均 PTS monotonic。Gate 3 墙钟 906.008 秒、最终媒体 PTS 906.186 秒；最小化、隐藏样本窗 PTS/墙钟比均约 1.000。一次 1.6 秒 worker 调度停顿触发既有 latest-pending coalescing drop 87（总 Capture 的 0.18%），随后约 12 分钟不再增长；Tap queue drop=5、no-free=0，NV12 starvation/reject/write failure=0，无 visibility 相关持续背压恶化。

## 11. PRESENT BEHAVIOR

P0 visibility events与动作时间一致。Gate 3 最小化和隐藏段 PresentDelta 均为 0；中途/最终恢复段 Present 分别 +1,693/+7,618。不可见不再计作 occluded/present/drop。

## 12. FINALIZE / VALIDATION / PUBLISH

三道 Gate 均正常 Stop；Gate 3 Encoder=Completed、OutputSuccess=true、FinalizeCount=1、Finalize HRESULT=0、SourceReaderValidation=PASS、Published=true，MP4 243,183,910 bytes。P2.6 Stop/Finalize/Validation/Publish 语义未改。

## 13. RESOURCE CLEANUP

Gate 3 Tap Outstanding/OutstandingAtShutdown=0，ResidualOutstandingAtShutdown=0，DoubleReturn=0，InvalidStateTransition=0，ConsumerConflict=0；结束后相关进程为零。

## 14. WGC ENVIRONMENT STATUS

`NOT-REPRODUCED / WGC-NORMAL-IN-THIS-RUN`。本轮专用测试及 2/5/15 分钟 Gate 均未出现 `0x80070424`；历史状态仍保留为 `ENVIRONMENT-BLOCKED / RELEASE-CANDIDATE-RECHECK`，不宣称永久清除。

三次 LongRun summary 仅因候选提交前工作区按施工顺序必然非 clean，使其通用 `git-reproducibility` 元数据门为 false、总 verdict 显示 BLOCKED；其余必需 gate 和全部 P2.7B 产品事实均 PASS，且 start/end status byte-for-byte 未变化。

## 15. REMAINING P2.7C WORK

普通 Close、录制中 Close、托盘/隐藏入口及完整窗口生命周期语义仍属于 P2.7C；本切片未修改 `MainForm`、`RecordingController` 或 `PreviewLifecycleController`。

## 16. FINAL VERDICT

`P2.7B-IMPLEMENTATION-READY`。这是 P2.7B 候选实现，不代表 P2.7 整体 FROZEN。

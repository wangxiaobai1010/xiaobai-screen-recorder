# P2.7 Background Recording Freeze

- Freeze date: 2026-08-08
- P2.6 frozen ancestor: `05ae40e9a939e59210044f54fe15c653f0e15ab5`
- P2.6 tag peel: `p2.6-safe-recording-recovery-pass-2026-08-08^{}` = `05ae40e9a939e59210044f54fe15c653f0e15ab5`
- P2.7 implementation candidate: `b7726f9b2cd86261d9289497463040550af30876` (`feat(recording): decouple recording from preview visibility`)

## Frozen decisions and evidence

1. **Mature solution.** `P2.7-MATURE-SOLUTION-AUDIT.md` selected the existing free-threaded WGC worker and the minimal fix: visibility controls presentation only.
2. **Implementation.** `P2.7-BACKGROUND-RECORDING-IMPLEMENTATION.md` records the P2.7B result: each WGC frame continues through `OutputCanvas` and the sole `RenderFrameTap` before the optional preview draw/Present tail.
3. **Independent review.** `P2.7B-INDEPENDENT-REVIEW.md` and the subsequent new-context final spot-check found no reachable visibility gate before `OutputCanvas`/tap, no second capture/render/recording pipeline, and no new ownership or shutdown blocker.
4. **Close decision.** `P2.7C-CLOSE-LIFECYCLE-AUDIT.md` concluded `P2.7C-NO-CODE-CHANGE-READY`: normal close reuses the existing managed single-flight Stop/Finalize/Validation/Publish path before Preview/D3D disposal.

## Requirement coverage

- Minimize and hide do not stop Recording; recorder-window visibility controls only preview presentation.
- Recording is downstream of `WGC frame -> OutputCanvas -> RenderFrameTap -> Encoder`; restore resumes preview presentation on the same path.
- Gate 3 confirms stable hidden/minimized background recording, while Stop, Finalize, Validation, Publish and `ResidualOutstanding = 0` remain intact.
- Visible, minimized and hidden Close use the same single-flight ownership; visibility does not change Close recording semantics.
- Actual crash recovery remains owned by the frozen P2.6 Manifest, Startup Scan and Recovery design.
- No second Capture, Recording or Recovery pipeline and no unnecessary public ABI were introduced.

## Background-recording evidence scope

The long-duration P2.7 evidence is the real P2.7B Gate 3 run of about 15 minutes (`906.008 s` wall time; `906.186 s` final recording PTS), including roughly 280 seconds minimized and 450 seconds hidden. In both disabled-presentation intervals, capture and PTS continued while Present stayed at zero; the run finalized, validated and published successfully with zero outstanding resources. This is supported together with the already frozen prior long Capture/Encoder baseline. It is **not** a claim that a 60-minute hidden test was run.

## WGC environment status

The historical environment item remains `ENVIRONMENT-BLOCKED / RELEASE-CANDIDATE-RECHECK`. P2.7 observations are only `NOT-REPRODUCED / WGC-NORMAL-IN-THIS-RUN`; this freeze does not remove the release-candidate recheck.

## Included files and validation discipline

- P2.7 implementation code, test and the two initial stage reports are in `b7726f9`.
- This evidence-only commit tracks `P2.7B-INDEPENDENT-REVIEW.md`, `P2.7C-CLOSE-LIFECYCLE-AUDIT.md`, and this freeze report. No runtime media, temporary output, build artifact or unrelated file is included.
- This closure intentionally does not repeat Gate 3, 30/60-minute runs, the P2.6 crash matrix, historical gates, full build, or full regression: the candidate is unchanged from the reviewed implementation and P2.7C made no code change.

## Final freeze verdict

`P2.7-BACKGROUND-RECORDING-FROZEN`

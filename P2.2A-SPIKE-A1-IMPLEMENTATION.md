# P2.2A Spike A1 实施记录

## 实验问题

在当前 Windows 11（10.0.22631）与 NVIDIA Quadro M2000 环境中，验证 Media Foundation Sink Writer 能否直接接收由独立 D3D11 Device 创建、GPU 生成且以 `MFCreateDXGISurfaceBuffer` 包装的 BGRA 纹理 Sample，并依靠 Sink Writer 自动转换生成可重新打开的 H.264 MP4。

本实验是隔离技术 Spike，不是正式录制功能，也不代表 Media Foundation 已成为最终产品路线。

## 非目标

本轮不接入 WGC、PreviewEngine、PreviewRenderer、MainForm、Camera、Follow、Cursor、RenderFrameTap、RecordingController、录制 UI、音频、暂停/恢复或正式 OutputSession。没有显式 BGRA→NV12、Video Processor、显式 Encoder MFT、FFmpeg、staging texture、GPU Map 或 CPU 完整帧回读。

不接 WGC 和 Preview 的原因是先把“GPU BGRA Sample 能否经自动转换完成 H.264 MP4”这一项能力单独证实，避免把捕获、预览、镜头或产品生命周期问题混入实验结论。Sink Writer 只存在于 Spike 路径，未进入产品运行文件。

## 项目与固定参数

- 项目：`spikes/P2.2A-MfSinkWriterGpuFrame/P2.SpikeA.MfSinkWriterGpuFrame.vcxproj`
- 构建：Release x64，独立 MSBuild，不加入主 solution
- 画面：1920×1080、30/1 fps、150 帧、约 5 秒
- 输入：`DXGI_FORMAT_B8G8R8A8_UNORM` / `MFVideoFormat_ARGB32`
- 输出：H.264、MP4、8,000,000 bps、progressive、无音频
- 纹理池：默认 6 个，正式实验固定使用 6
- 输出：`artifacts/spikes/P2.2A`

## GPU 测试画面

`D3DTestFrameProducer` 使用 Render Target、`ClearRenderTargetView` 和 `ID3D11DeviceContext1::ClearView` 在 GPU 上直接生成固定 RGB 区块与随帧移动的白色矩形。Alpha 恒为 1。CPU 不创建、上传、读取或校验完整帧像素。

## D3D11 Device 与 Device Manager

Device flags 为：

- `D3D11_CREATE_DEVICE_BGRA_SUPPORT`
- `D3D11_CREATE_DEVICE_VIDEO_SUPPORT`

优先枚举第一个非软件适配器；只有硬件 Device 创建失败时才使用 WARP，且日志会单独标记。通过 `ID3D10Multithread::SetMultithreadProtected(TRUE)` 开启多线程保护。

Media Foundation 侧调用 `MFCreateDXGIDeviceManager` 和 `ResetDevice`。Sink Writer attributes 设置 `MF_SINK_WRITER_D3D_MANAGER` 与 `MF_READWRITE_ENABLE_HARDWARE_TRANSFORMS=TRUE`。这只证明设备管理器已提供给自动管线；不能由此断言实际编码器身份。

## Sample 包装链

每帧链路为：

1. 从 Recorder-owned 有界池取得 Free slot；
2. 状态进入 GPUProducing；
3. GPU 直接渲染到 slot 的 BGRA 纹理；
4. `MFCreateDXGISurfaceBuffer` 包装纹理；
5. 读取 DXGI buffer 的 MaxLength 并设置 CurrentLength；
6. `MFCreateTrackedSample`，查询 `IMFSample`；
7. 添加 buffer，设置 Time 与 Duration；
8. `IMFTrackedSample::SetAllocator` 绑定该 slot 的线程安全回调；
9. 状态进入 SubmittedToMF；
10. `IMFSinkWriter::WriteSample`；
11. 仅在 tracked callback 到达后进入 Returned，再回到 Free。

`WriteSample` 返回本身不释放 slot，也不会触发提前复用。

## Tracked Sample 所有权与池状态

固定池仅创建 6 个 1920×1080 BGRA 纹理。状态为 Free、GPUProducing、SubmittedToMF、Returned。每个 slot 使用独立 `IMFAsyncCallback`；回调持有的 pool 引用为受生命周期约束的原始引用，pool 在 writer 释放、全部 tracked callback 返回之后才析构。所有状态计数在 mutex 下更新，slot 不会在 SubmittedToMF 时被覆盖。

本机 Sink Writer 在 `Finalize` 成功后仍持有最后的输入 Sample 引用。因此正常路径先确认 `Finalize` 成功，再释放已经 finalized 的 writer，随后等待所有 tracked callback；没有调用 `Flush`。这是为了让 Media Foundation 实际释放引用，并不提前释放底层纹理。

## CFR 时间戳

时间戳只用整数计算：

```text
PTS(n)      = floor(n × 10,000,000 / 30)
Duration(n) = PTS(n+1) - PTS(n)
```

n 为 0..149。首帧 PTS=0，末帧 PTS=49,666,666，总时长=50,000,000 个 100ns 单位。墙钟仅用于约 30fps 提交节奏，不参与媒体时间戳。

## Stop 与 Finalize 顺序

完成第 150 帧后停止产生新帧，调用 `NotifyEndOfSegment`，再调用 `Finalize`。Finalize 成功后释放 writer 使其归还输入 Sample，等待 outstanding tracked sample 归零，然后释放 Device Manager。失败时保留 partial MP4 和 JSON，不执行自动删除，也不切换 NV12 或 CPU 路径。

## Source Reader 验证

`Mp4Validator` 在 Finalize 后使用 `MFCreateSourceReaderFromURL` 重新打开 partial MP4，核验文件非空、原生视频轨为 H.264、1920×1080、时长约 5 秒、时间戳单调、样本数约 150，并持续读取到 EOS。全部通过后才把 `.partial.mp4` 原子提升为最终 `.mp4`。

## H.264 候选与身份边界

Spike 使用 `MFTEnumEx` 仅做诊断枚举，记录 Friendly Name、CLSID、Hardware URL、async/hardware 属性和 Adapter LUID 属性是否存在，不显式激活或绑定任何候选。`MF_SINK_WRITER_D3D_MANAGER` 和硬件转换属性不能证明 Sink Writer 最终选中的 encoder；实际 Encoder 身份必须报告为 **UNKNOWN**。

## 三次运行

最终正式三次独立生命周期批次为 `20260730T151632455Z-54619a8a`，证据由 `RUN-P2.2A-SPIKE.ps1` 生成到 `artifacts/spikes/P2.2A`。每次都重新完成 COM、MF、D3D、Device Manager、Sink Writer、纹理池、Finalize 和 Source Reader 生命周期。

| Session | Exit | Submitted / Dropped | Starvation | Pool high | Returned / Outstanding | MP4 bytes | Validation |
|---|---:|---:|---:|---:|---:|---:|---|
| `...-run1` | 0 | 150 / 0 | 0 | 2 | 150 / 0 | 1,583,981 | H.264, 1920×1080, 5s, 150 samples, EOS |
| `...-run2` | 0 | 150 / 0 | 0 | 2 | 150 / 0 | 1,583,981 | H.264, 1920×1080, 5s, 150 samples, EOS |
| `...-run3` | 0 | 150 / 0 | 0 | 2 | 150 / 0 | 1,583,981 | H.264, 1920×1080, 5s, 150 samples, EOS |

三次分类均为 `SUPPORTED`，均无残留 Spike 或 XbPreview 进程。更早的诊断运行和一次因 PowerShell UTF-8 路径误解码产生的无效汇总只保留为诊断材料，不拼接进此正式批次。

## 已确认的关键 HRESULT

诊断性预运行确认：`CreateWriter`、`AddStream`、`SetInputMediaType`、`BeginWriting`、150 次 `WriteSample`、`NotifyEndOfSegment`、`Finalize` 和 Source Reader 验证均可返回 `S_OK`。首次诊断曾因未设置 DXGI buffer CurrentLength 在 `WriteSample` 得到 `E_INVALIDARG (0x80070057)`；依据 buffer 合同补齐 MaxLength/CurrentLength 后，该失败消失。这是 Harness 修正，不是 NV12 或 CPU 回退。

## 自动 BGRA 路径判定

正式判定为 `SUPPORTED`：本机自动 BGRA 路径连续三次全通过，每次 150 帧零丢失、零 starvation、tracked sample 全归还，Finalize 与 Source Reader 全通过，并生成三个独立有效 MP4。该结论只适用于当前机器与当前实验条件。

## 性能与资源证据

| Run | WriteSample P50 / P95 / Max (ms) | Slot wait P95 (ms) | Finalize (ms) | Validation (ms) | Total (ms) |
|---:|---:|---:|---:|---:|---:|
| 1 | 0.0167 / 0.0301 / 0.0414 | 0.0013 | 5.2197 | 20.3002 | 5,398.4286 |
| 2 | 0.0138 / 0.0289 / 0.0504 | 0.0014 | 5.2701 | 19.2707 | 5,389.5452 |
| 3 | 0.0147 / 0.0295 / 0.1468 | 0.0013 | 5.2494 | 19.6928 | 5,379.8112 |

Pool high watermark 三次均为 2，固定分配仍为 6，未出现无界纹理分配。Artifacts 不进入正式文件集合。

## 未解决与 UNKNOWN

- 实际 Encoder 身份：UNKNOWN；
- Sink Writer 是否最终选择了枚举出的 NVIDIA hardware MFT：UNKNOWN；
- B-frames=0 属性未通过当前自动路径确认应用；
- 显式 GPU BGRA→NV12 对照尚未进行；
- 不代表其他 GPU、驱动或 Windows 构建均支持；
- 未验证 WGC 真实帧、RenderFrameTap、正式 RecordingController、音频、暂停/恢复或安全 OutputSession。

## 下一步

如果正式三次运行均通过，只能确认本机自动 BGRA Spike 路径可行，仍不得直接进入 RenderFrameTap；应先审查证据是否足以设计下一隔离实验。若自动路径稳定地在明确阶段失败，下一步应独立设计显式 GPU BGRA→NV12 对照。若所有权、验证器或 Harness 自身失败，则先修正 Harness，不能把结果解释为路径不支持。

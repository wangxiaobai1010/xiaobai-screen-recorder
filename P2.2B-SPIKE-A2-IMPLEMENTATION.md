# P2.2B Spike A2 实施记录

## 1. 实验问题与结论边界

本实验只验证以下隔离路径：

`GPU BGRA 测试画面 → D3D11 Video Processor → 固定 NV12 池 → DXGI Media Buffer → Sink Writer NV12 输入 → H.264 MP4`

A1 已证明 Sink Writer 可以接受 GPU BGRA 并自动完成后续转换；A2 则把 BGRA→NV12 明确放在 Recorder 所有的 D3D11 Video Processor 路径中，用于比较格式能力、资源所有权、颜色和提交开销。A2 没有接入 WGC、Preview、UI、Camera、Follow、Cursor、RenderFrameTap、RecordingController 或音频，也没有加入主 Solution。

本实验结论不是正式录制完成，也不代表所有机器支持这条路径。

## 2. 工程与边界

- 独立项目：`spikes/P2.2B-D3D11VideoProcessorNv12/P2.SpikeA2.D3D11VideoProcessorNv12.vcxproj`
- 构建：Release x64，MSVC v143，Windows SDK
- 输出：`artifacts/spikes/P2.2B`
- A1 正式源码：只读复用公共头和 MP4 验证实现，未修改
- 产品运行文件：未修改，ProductRuntime 保持 73 文件
- 第三方库、FFmpeg、CPU 编码前整帧转换：无

## 3. D3D11 Device 与 Video Processor

设备创建标志为 `D3D11_CREATE_DEVICE_BGRA_SUPPORT | D3D11_CREATE_DEVICE_VIDEO_SUPPORT`，并启用 `ID3D10Multithread::SetMultithreadProtected(TRUE)`。本机正式三轮均得到：

- Adapter：NVIDIA Quadro M2000
- Adapter LUID：`0:5DFA8`
- VendorId：4318
- DeviceId：5168
- Feature Level：11_0
- Device flags：2080
- Hardware：true；WARP：false
- `ID3D11VideoDevice` / `ID3D11VideoContext`：可用
- BGRA Video Processor input：支持
- NV12 Video Processor output：支持
- Enumerator、Processor、InputView、OutputView：均 `0x00000000`

能力判定同时使用 `CheckVideoProcessorFormat` 和实际视图创建结果，不以“对象能创建”替代格式查询。

## 4. GPU 测试画面与颜色设置

BGRA 源为 `DXGI_FORMAT_B8G8R8A8_UNORM` 1920×1080 纹理，由 GPU `ClearRenderTargetView/ClearView` 生成：

- 不透明红、绿、蓝固定色块；
- 固定灰阶与深黑背景；
- 每帧移动的白色矩形；
- 离线验证所需的固定取样位置。

编码主链没有 CPU 逐帧生成或读取完整图像。SourceRect、DestinationRect 和 OutputTargetRect 均为 `[0,0,1920,1080]`，progressive，alpha 不透明。输入设置为 RGB full SDR 语义，输出目标设置为 BT.709 limited SDR。旧式 `D3D11_VIDEO_PROCESSOR_COLOR_SPACE` 位域在具体驱动中的精确映射不能仅靠 API 回读严格证明，故该精确解释继续标记 `UNKNOWN`。

## 5. NV12 池、所有权和线程顺序

固定创建 6 个 `DXGI_FORMAT_NV12` 纹理。slot 状态为：

`Free → GPUConverting → SubmittedToMF → Returned`

每帧只有在 slot 为 Free/Returned 时才能取得；`VideoProcessorBlt` 完成提交后，将该 NV12 纹理用 `MFCreateDXGISurfaceBuffer` 包装，并通过 `IMFTrackedSample` 的 allocator callback 归还。`WriteSample` 返回不代表可复用，SubmittedToMF slot 不会被覆盖，也不会按帧无限分配新纹理。

测试画面生成、Video Processor Blt 和 Sink Writer 提交均在同一 MTA 工作顺序、同一个 GPU command owner 上串行执行，不会由多个线程无序调用 Immediate Context。正常路径没有 `Flush`、busy wait、staging、Map 或整帧 readback。

## 6. Sink Writer、时间戳与结束顺序

- 输入：`MFVideoFormat_NV12`
- 输出：`MFVideoFormat_H264`
- 1920×1080、30/1 fps、progressive、1:1 PAR、8,000,000 bps
- `MF_SINK_WRITER_D3D_MANAGER`
- `MF_READWRITE_ENABLE_HARDWARE_TRANSFORMS=TRUE`
- PTS：`floor(n × 10,000,000 / 30)`，n=0..149
- Duration：`PTS(n+1)-PTS(n)`

结束顺序为：提交第 150 帧 → `NotifyEndOfSegment` → `Finalize` → 释放 Sink Writer → 等待所有 Tracked Sample callback → 确认 Outstanding=0 → Source Reader 验证 → 离线颜色验证 → 将 partial 原子提升为最终 MP4。

Sink Writer 实际选择的 Encoder 身份仍为 `UNKNOWN`。没有用可枚举候选冒充实际选择，也没有声称 B-frame=0 已被确认应用。

## 7. 输出与离线颜色验证

第一层 Source Reader 验证 H.264、1920×1080、5 秒、150 samples、时间戳单调和 EOS。第二层仅在编码结束后请求 RGB32，读取首帧、中间帧和末帧的少量像素，检查：

- 红、绿、蓝通道没有交换；
- 灰阶；
- 深黑背景和白位；
- 移动白色矩形在三帧的预期位置。

颜色容差为每通道绝对误差 60。正式三轮最大误差均为 1，平均误差均为 0.3333，状态均为 PASS。该 CPU 读取只属于输出验证，不参与编码主链。

## 8. 三次 A2 正式运行

正式批次：`20260730T155805866Z-c7ca6709`

| Run | Submitted | Dropped | Starvation | High watermark | Returned | Outstanding | Finalize | Source Reader | Color |
|---:|---:|---:|---:|---:|---:|---:|---|---|---|
| 1 | 150 | 0 | 0 | 2 | 150 | 0 | S_OK | PASS | PASS |
| 2 | 150 | 0 | 0 | 2 | 150 | 0 | S_OK | PASS | PASS |
| 3 | 150 | 0 | 0 | 2 | 150 | 0 | S_OK | PASS | PASS |

每个最终文件均为 H.264、1920×1080、50,000,000 个 100ns 单位、150 samples、时间戳单调并到达 EOS；最终文件各 49,794 bytes。正式三轮 partial 文件均不存在，失败阶段为空，HRESULT 为 `0x00000000`。

## 9. A1 回归

完成 A2 三轮后，以独立批次 `20260730T155840867Z-f297593b` 运行一次 A1：

- A1 Build：PASS
- 分类：SUPPORTED
- Submitted：150/150
- Dropped：0
- Returned：150
- Outstanding：0
- Finalize：PASS
- Source Reader：PASS
- A1 正式源码：未修改

该回归输出未覆盖 A1 原正式三轮批次。

## 10. A1/A2 性能对照

下表为 A1 正式三轮批次 `20260730T153137990Z-09b70fb4` 与 A2 正式三轮的三轮均值：

| 指标 | A1 自动 BGRA | A2 显式 NV12 |
|---|---:|---:|
| WriteSample P50 (ms) | 0.0186 | 0.0319 |
| WriteSample P95 (ms) | 0.0378 | 0.0575 |
| WriteSample Max (ms) | 0.0682 | 0.1113 |
| SlotWait P95 (ms) | 0.0014 | 0.0014 |
| Pool high watermark | 2 | 2 |
| Finalize (ms) | 6.4130 | 8.2771 |
| Source Reader container validation (ms) | 22.7023 | 20.3971 |
| Total (ms) | 5399.4572 | 10953.0566 |
| File size (bytes) | 1,583,981 | 49,794 |

A2 额外记录：

- VideoProcessorBlt 返回耗时 P50/P95/Max：0.0062 / 0.0125 / 0.0262 ms
- 离线颜色验证：5499.7874 ms
- Process CPU time：5229.1667 ms
- Peak Working Set：238,107,307 bytes

WriteSample、slot wait 和 high watermark 没有明显恶化。A2 的 Total 包含约 5.56 秒的完整离线解码颜色验证，不能直接用其总时长推断编码主链比 A1 慢。A1 未记录 Process CPU time 和 Peak Working Set，因此这两项的 A1/A2 对比为 `UNKNOWN`。A1/A2 测试图案不同，文件大小不能用于推断编码效率。`VideoProcessorBlt` 计时只代表 CPU 提交返回时间，不是 GPU 执行时长。

## 11. 自动门禁与正式集合

`SELF-TEST-P2.bat SpikeA2` 已完成：

1. 残留进程检查；
2. VERIFY Baseline / SpikeA1 / SpikeA2；
3. 主 Solution Release x64 Build；
4. Managed Tests；
5. Native Tests；
6. A1/A2 项目 Build；
7. A2 连续三次；
8. A1 独立回归一次；
9. 最终静态、ProductRuntime、上游冻结源与残留进程复核。

唯一输出为 `SELF-TEST-P2 SpikeA2: PASS-SUPPORTED`。正式文件集合由门禁真实枚举；构建产物、MP4、partial、日志和运行 JSON 均在 artifacts 下，不进入正式集合。

## 12. UNKNOWN、下一步条件与正式结论

继续为 `UNKNOWN`：

- Sink Writer 实际选择的 Encoder MFT 身份；
- 驱动对旧式 Video Processor 颜色位域的更精确内部解释；
- `VideoProcessorBlt` 对应的真实 GPU 执行耗时；
- 接入真实 Preview 共享纹理后的跨线程同步要求；
- 其他 GPU/驱动是否支持同一路径。

本机已经具备进入真实 RenderFrameTap 设计前的基本转换证据：显式 Video Processor BGRA→NV12、固定有界池、Tracked Sample 归还、NV12 Sink Writer、Finalize、输出与颜色验证均连续三次成功。但本轮不开始 RenderFrameTap；在正式接入前仍需设计共享纹理所有权、背压、设备丢失和 Preview/Recorder 生命周期隔离。Encoder 身份精确绑定不是本次证据成立的阻断项，但若后续产品必须强制指定硬件编码器，则需独立实验。

**实验判断：SUPPORTED**

**最终结论：PASS：P2.2B Spike A2显式GPU BGRA→NV12路径验证成功**

# P2.3B RenderFrameTap 与有界 GPU 帧交接

## 1. 阶段目标

P2.3B 只建立默认关闭的内部 GPU 帧交接边界。它不实现录制、编码、文件、音频、产品按钮或公开 ABI。

正式预览链保持为：

```text
WGC → Crop / Camera → 固定 BGRA OutputCanvas → Custom Cursor
    → RenderFrameTap（可选、默认关闭）
    → Preview Letterbox Blit → Present
```

Tap 得到的是固定 OutputCanvas 内容：已经包含 Crop、Camera、Follow 的结果和 Custom Cursor，不包含 Preview 黑边，也不受 Preview 窗口 Resize 影响。

## 2. 只读审计与插入点

- WGC 回调：`PreviewEngine::OnFrameArrived`。
- 原始时间戳：`Direct3D11CaptureFrame.SystemRelativeTime().count()`，以 100ns 值保存；值无效时显式标记 Missing，不生成 CFR 时间戳。
- Render Worker：`PreviewEngine::WorkerLoop` → `ProcessPendingFrame` → `PreviewRenderer::RenderFrame`。
- OutputCanvas 背景完成：`DrawFullscreenPass(outputCanvas_.RenderTargetView(), ...)`。
- Cursor 完成：`customCursorRenderer_.Draw(..., outputViewport)` 返回之后。
- Preview 呈现起点：`ClearRenderTargetView(previewRenderTargetView_)` 和随后 OutputCanvas SRV 的 Letterbox Blit。
- 最终 Tap 插入点：Cursor 完成之后、Preview Back Buffer 清黑之前。

## 3. 模块和所有权

`RenderFrameTap.h/.cpp` 独立承担：

- Recorder-owned GPU Texture2D 池；
- 预分配有界环形队列；
- Generation 与 Slot 状态；
- move-only `GpuFrameLease` 一次性归还；
- 独立诊断消费者；
- 有界内存诊断事件和 Shutdown JSONL 摘要。

`PreviewRenderer` 拥有 `RenderFrameTap`，因此 Tap、OutputCanvas 和 D3D Device 位于同一个 Render Worker 生命周期。消费者只读取帧元数据并持有池纹理引用，不调用 Immediate Context、不执行 CPU 全帧读取。

## 4. 固定池和有界队列

- Active PoolSize：严格为 6。
- QueueCapacity：4。
- 每个 Active Generation 只创建 6 张与 OutputCanvas 同宽高、同 BGRA8 格式的 GPU Texture2D。
- Queue 使用固定 `std::array` 环形存储，不会无限增长。
- 每个 Generation 最多允许两个尚有 ConsumerOwned Lease 的 retired generation；超过该边界时 Drop-New，不继续创建纹理池。
- Generation 切换期间，旧 ConsumerOwned 纹理可暂时存活到 Lease 归还；这不会改变新 Active Pool 的 6 槽上限。

Slot 状态只有：

```text
Free → Queued → ConsumerOwned → Free
```

Lease Token 同时校验 Slot 和一次性归还。错误状态、重复归还和迟到归还分别计数。

## 5. Render 线程非阻塞约束

Render 路径使用 `std::try_to_lock`，绝不等待 Consumer Mutex。任何以下状态都丢弃当前新 Tap Frame：

- 锁正在被 Consumer 使用；
- Queue 已满；
- 没有 Free Slot；
- OutputCanvas Generation 正在切换或不匹配；
- Tap Disabled / Stopping。

策略严格为 Drop-New：不覆盖旧帧、不等待空位、不等待纹理、不 Sleep、不调用 condition-variable wait，也不执行磁盘日志写入。

GPU 交接使用 Immediate Context 上的 `CopyResource(poolTexture, outputCanvas)`。消费者永远不会长期持有仍会被下一帧重写的 OutputCanvas 本体。

## 6. Generation 生命周期

以下边界创建新 Generation：

- 第一次启用；
- OutputCanvas Width / Height / Format 变化；
- Session Stop 后再次 Start；
- Renderer / Device 重建；
- Tap Stop 后重新启用。

Preview SwapChain `ResizeBuffers` 不改变 OutputCanvas 描述，因此不会切换 Generation。

切代顺序：阻止不匹配帧入队 → 冲洗旧 Queue → 归还旧 Queued Slot → 保留仍 ConsumerOwned 的旧 Generation → 创建新的 6 槽池 → 丢弃切代帧 → 下一帧开始使用新 Generation。迟到 Lease 只归还其所属旧 Slot，不会进入新池。

## 7. 时间戳

每个成功入队帧记录：Pool Slot、Width、Height、DXGI Format、Generation、Frame Sequence、TimestampValid、原始 SystemRelativeTime100ns 和 Enqueue QPC。

- 有效 WGC 时间戳原样传递；
- Missing 只计数；
- 同一 Generation 内的倒退计入 Regression；
- 不生成固定 30 FPS、CFR 或任何替代时间戳。

## 8. 默认关闭和内部诊断入口

产品默认不启用 Tap，不创建纹理池、不创建 Consumer Thread、不 Copy、不改变 Preview。

仅用于内部实机诊断的环境变量：

```text
XB_PREVIEW_DIAGNOSTIC_TAP=normal
XB_PREVIEW_DIAGNOSTIC_TAP=slow
XB_PREVIEW_DIAGNOSTIC_TAP_DELAY_MS=0..5000
```

该入口不是 UI、不是设置、不是公开 ABI。启用后，Shutdown 在现有 diagnostic-logs 目录写入：

```text
p2.3b-tap-{native-session-id}.jsonl
```

Shutdown summary 从消费者线程实际使用的同一个、已经解析完成的
`RenderFrameTapConfiguration` 记录：

- `ConsumerMode`：`"normal"` 或 `"slow"`；
- `ConsumerDelayMs`：应用可选且有界的延迟覆盖之后，实际生效的毫秒数。

序列化时不会重新读取环境变量，也不会根据消费帧率反推模式。

## 9. Shutdown 顺序

1. `enabled=false`、`stopping=true`；
2. 冲洗有界 Queue；
3. 唤醒诊断 Consumer；
4. Consumer 立即结束延迟并归还当前 Lease；
5. Join Consumer；
6. 记录 OutstandingAtShutdown、ShutdownDuration 和最终摘要；
7. 释放 Active Pool 的 Device/Context 所有权；
8. `PreviewRenderer` 继续执行原有 Cursor、ClearState/Flush、OutputCanvas、SwapChain 和 Device 清理。

外部测试暂持 Lease 时 Shutdown 不等待；Lease 通过共享状态安全迟归还。正式产品诊断消费者由 Tap 自己拥有，因此正常停止后 OutstandingAtShutdown 应为 0。

## 10. 诊断指标

实现记录提示词要求的全部核心指标，包括 Observed、Copied、Enqueued、Consumed、Returned、四类 Drop、Timestamp Valid/Missing/Regression、Queue/Free/ConsumerOwned/Outstanding、Generation Change、Stale Flush、Late Return、Double Return、Invalid Transition 和 Shutdown Duration，并额外记录 LockBusy、TexturesCreated 和固定容量 Generation Event Ring。

## 11. 自动测试

Native tests 覆盖：

- 默认 Disabled 零资源活动；
- 元数据和独立 GPU Copy；
- QueueFull 与 NoFreeSlot Drop-New；
- Geometry 切代和旧 Lease 迟归还；
- 相同 OutputCanvas 下的 Preview Resize 等价活动不切代；
- 有效、缺失和倒退时间戳；
- 100ms 慢消费者不阻塞 Producer；
- Stop / Start 五轮 Generation 单调；
- Shutdown 时持帧不死锁；
- 可选 `--p2.3b-diagnostics` 30 秒正常消费测试。

静态门禁检查 Tap 的准确调用顺序、固定常量、try-lock、固定数组队列、CopyResource、默认关闭、环境入口、项目文件、禁止 API 和 P2.3A 历史指纹。

## 12. 非目标

P2.3B 没有 Media Foundation、H.264、NV12、MP4、Sink Writer、Source Reader、RecordingController、录制 UI、公开 ABI、C# PInvoke、音频、Pause/Resume、文件保存、CPU 全帧读取或正式编码消费者。

P2.3B 实现完成不等于用户视觉验收，不等于正式冻结，也不表示录制或编码链路已经完成。

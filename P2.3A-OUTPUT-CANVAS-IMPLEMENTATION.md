# P2.3A 固定 OutputCanvas 与 Preview 呈现解耦

## 1. 阶段目标

P2.3A 首次修改正式产品 GPU 渲染主链，建立：

`WGC → Crop/Camera → 固定 BGRA8 OutputCanvas → Custom Cursor → Preview Blit/Letterbox → SwapChain Present`

正式合成不再直接写入 Preview SwapChain Back Buffer。Preview Resize 只调整窗口交换链及最终呈现，不改变 OutputCanvas 尺寸或内容语义。

## 2. 修改前真实调用链

- `PreviewEngine::OnFrameArrived` 把最新 WGC frame 放入 single-frame slot。
- Render Worker 的 `PreviewEngine::ProcessPendingFrame` 取得 WGC `ID3D11Texture2D`、Camera snapshot 和 Cursor state。
- `PreviewRenderer::RenderFrame` 把 WGC texture复制到 `sourceTexture_`。
- 同一个 Crop/Camera shader此前直接画入 SwapChain Back Buffer的Letterbox viewport。
- `CustomCursorRenderer::Draw` 随后直接画入该Back Buffer。
- `IDXGISwapChain1::Present` 完成窗口呈现。

Back Buffer由 `PreviewRenderer::CreateSwapChain/CreateBackBuffer` 创建，并由 `PreviewRenderer::Resize` 的 `ResizeBuffers` 重建。Stop时，Engine先停止FramePool和回调，再调用 `PreviewRenderer::Shutdown`。

## 3. OutputCanvas 所有权

新增 `OutputCanvasTarget`，由 `PreviewRenderer` 独占，并仅由Render Worker访问。它持有：

- `DXGI_FORMAT_B8G8R8A8_UNORM` Texture2D；
- Render Target View；
- Shader Resource View；
- 固定width/height/format描述；
- 资源generation。

纹理同时使用 `D3D11_BIND_RENDER_TARGET | D3D11_BIND_SHADER_RESOURCE`。`Ensure`只在SessionGeometry的OutputCanvas描述变化时重建；Preview窗口尺寸不是其输入，因此普通Preview Resize不会触发OutputCanvas重建。

## 4. 两阶段 GPU 渲染

### 正式合成阶段

1. WGC texture GPU copy到Renderer自有 `sourceTexture_`；
2. 确保固定尺寸OutputCanvas存在；
3. 清理OutputCanvas；
4. 在完整OutputCanvas viewport中执行Crop和Camera shader；
5. 在同一个OutputCanvas viewport中绘制Custom Cursor。

### Preview Letterbox 阶段

1. 清理SwapChain Back Buffer为黑色；
2. 仅根据OutputCanvas宽高和Preview宽高计算Letterbox；
3. 用identity transform把OutputCanvas SRV缩放绘制到Back Buffer的Letterbox viewport；
4. 解除SRV绑定；
5. `Present(1,0)`。

Preview专用黑边和Letterbox从未写入OutputCanvas。

## 5. Crop、Camera、Follow与OutputCanvas

`XbPreviewSessionGeometryV1`原有ABI已经包含独立的：

- CaptureRegion：`captureLeft/top/width/height`
- OutputCanvas：`outputWidth/outputHeight`

P2.3A不修改ABI。`ResolveCropTransform`继续从CaptureRegion生成源UV，但其输出目标尺寸现在严格取自 `geometry.outputWidth/outputHeight`，不再错误回填capture尺寸。Camera/Follow的既有CameraTransform和shader数学未改变。

## 6. Custom Cursor

Cursor shape、premultiplied alpha、POINT sampler、blend state、zoom比例和hotspot数学均未修改。变化仅是目标viewport：

- 修改前：Preview Letterbox viewport；
- 修改后：完整OutputCanvas viewport。

Cursor先合成进入OutputCanvas，之后与背景一起通过Preview blit缩放，因此最终窗口视觉比例保持与原路径等价；Preview Resize不会改变正式合成像素。

## 7. Resize与资源释放

`PreviewRenderer::Resize`仍只执行：

- 解除Back Buffer RTV；
- `ResizeBuffers`；
- 重建Preview Back Buffer RTV。

它不引用或修改 `outputCanvas_`。OutputCanvas仅在OutputCanvas描述变化或Renderer Shutdown时变化。

Shutdown顺序保持：

1. Engine停止接收frame；
2. 等待FrameArrived callback退出；
3. 关闭CaptureSession和FramePool；
4. 清理pending frame；
5. `PreviewRenderer::Shutdown`；
6. Cursor renderer释放；
7. D3D Context ClearState/Flush；
8. OutputCanvas、source texture、Back Buffer、SwapChain和Device依次释放。

## 8. 测试和静态证据

Native tests新增/加强：

- CaptureRegion UV与OutputCanvas尺寸相互独立；
- 1001×701 CaptureRegion可以输出到1280×720；
- OutputCanvas变化不改变Crop采样；
- BGRA8 OutputCanvas描述校验；
- 相同OutputCanvas描述保持稳定；
- 只有OutputCanvas几何改变才需要新画布；
- zero-size和非BGRA8 composition canvas被拒绝。

P2.3A静态门禁验证：

- 193文件P2.2B历史基线可重现；
- 上游P1d-a2冻结源和四份快照不变；
- 产品修改严格限制在OutputCanvas渲染链、Native测试和工程接线；
- composition pass先进入OutputCanvas；
- Custom Cursor进入OutputCanvas；
- Preview Letterbox只发生在第二pass；
- Preview Resize不接触OutputCanvas；
- ABI、WGC、Camera/Follow数学和Cursor视觉实现没有改变；
- 产品运行路径没有Media Foundation、H.264、RecordingController或RenderFrameTap。

## 9. 非目标

本阶段没有实现：

- Media Foundation或Sink Writer；
- H.264、MP4或音频；
- RecordingController、录制状态机或文件生命周期；
- RenderFrameTap；
- Encoder queue、NV12池或背压；
- Pause/Resume；
- Preview以外的导出验证；
- 新的Camera、Follow、Cursor或CaptureRegion产品规则。

## 10. RenderFrameTap 后续接口边界

OutputCanvas现在是尺寸稳定、包含Crop/Camera/Follow和Custom Cursor、且不含Preview Letterbox的Renderer-owned GPU纹理。它构成后续RenderFrameTap设计的正确接入点，但P2.3A没有暴露、持有或提交任何录制回调，也没有开始编码集成。

## 11. 验证结论边界

本阶段通过构建、Native/Managed tests、静态门禁和A1/A2隔离回归验证代码与资源边界。程序不会由自动门禁启动；窗口Resize、Camera、Follow、Cursor及Stop/Start的最终视觉等价性仍由用户实机复测确认。
